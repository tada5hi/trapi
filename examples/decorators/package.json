{
    "name": "@trapi/example-decorators",
    "version": "0.0.0",
    "private": true,
    "type": "module",
    "description": "Worked example: a custom decorator runtime + a v2 Preset that recognises it",
    "scripts": {
        "test": "vitest --config ./test/vitest.config.ts --run"
    },
    "dependencies": {
        "@trapi/core": "*",
        "@trapi/metadata": "*"
    },
    "devDependencies": {
        "vitest": "^5.0.0"
    }
}
