{
    "name": "@trapi/preset-built-in",
    "version": "0.0.0",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "devDependencies": {
        "@trapi/metadata": "^0.4.1"
    },
    "peerDependencies": {
        "@trapi/metadata": "^0.x"
    },
    "scripts": {
        "build": "rimraf dist && tsc -p tsconfig.build.json"
    }
}
