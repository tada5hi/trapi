{
    "name": "@trapi/preset-routup",
    "version": "0.0.0",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "dependencies": {
        "@trapi/preset-swagger": "^0.0.0"
    },
    "devDependencies": {
        "@trapi/metadata": "^0.4.1"
    },
    "peerDependencies": {
        "@trapi/metadata": "^0.x"
    },
    "scripts": {
        "build": "rimraf dist && tsc -p tsconfig.build.json"
    }
}
